<!--
  Bu snippet android/app/src/main/AndroidManifest.xml dosyasındaki <application> ETİKETİ
  İÇİNE eklenecek. `expo prebuild` sonrası bu adım manuel veya config plugin ile yapılır.
  (modules/widget/plugin/index.js bu işi otomatize etmek üzere tasarlandı.)
-->

<!-- Widget'ın layout/configuration sağlayıcısı -->
<receiver
    android:name="com.berberapp.widget.BarberWidgetProvider"
    android:exported="true">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/barber_widget_info" />
</receiver>

<!-- Widget tıklama olaylarını yakalayan receiver -->
<receiver
    android:name="com.berberapp.widget.BlockActionReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="com.berberapp.BLOCK_WALKIN" />
    </intent-filter>
</receiver>
