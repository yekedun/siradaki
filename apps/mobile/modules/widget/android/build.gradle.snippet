// Bu snippet `android/app/build.gradle` içindeki `dependencies { ... }` bloğuna eklenir.
// `expo prebuild` sonrası manuel veya config plugin ile.

dependencies {
    // ... mevcut bağımlılıklar ...

    // BlockActionReceiver içindeki HTTP çağrısı için
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")
}
