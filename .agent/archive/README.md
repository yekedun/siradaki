# Archive

Archive old material here to keep the active surface small.

Subfolders:

- `summaries/`
- `tasks/`
- `scratchpads/`
- `deprecated/`

Rule:

- archive instead of deleting when old context may still matter
- do not read archives unless the current task explicitly needs them
