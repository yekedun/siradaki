---
date: 2026-05-11
session: 03
focus: scheduling RLS hardening
tags:
  - scheduling
  - rls
  - supabase
status: blocked_remote
---

# Completed

- Retried remote verification for `20260513074317_backend_scheduling_hardening.sql`; one remote query confirmed no legacy `barber_id` function refs, but other remote checks hit auth/pooler failures.
- Added `20260514080010_scheduling_rls_policy_consolidation.sql`.
- Consolidated scheduling RLS policies for `appointments`, `blocks`, `staff`, and `staff_schedules`.
- Removed legacy scheduling policy names and reduced each scheduling table to four command-scoped policies.
- Replaced scheduling policy `auth.uid()` calls with initplan-safe `(SELECT auth.uid())`.

# Validation

- `supabase db reset` passed.
- `pnpm turbo type-check` passed.
- Local advisors dropped from 122 warnings to 28 warnings.
- Scheduling tables no longer report local advisor warnings.
- Seeded local scheduling probe passed: atomic appointment insert, overlapping block rejection, and appointment mirror creation.

# Blocked

- Remote dry-run/push is blocked by Supabase pooler `ECIRCUITBREAKER` after authentication failures.
- Next remote action should wait for the circuit-breaker window to clear and then use the correct `SUPABASE_DB_PASSWORD`.
