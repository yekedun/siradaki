---
date: 2026-05-11
session: 07
focus: backend scheduling hardening proof
tags:
  - backend
  - scheduling
  - supabase
  - testing
status: review
---

# Completed

- Added `pnpm backend:proof` and `pnpm backend:proof:fast`.
- Added deterministic SQL proof coverage for scheduling invariants:
  - staff schedule override
  - break blocking
  - closed day blocking
  - cancelled appointment rebooking
  - any-staff assignment
  - block vs booking conflict
  - appointment slot mirror sync
  - anon/authenticated RLS and RPC exposure
  - DST/local day boundary conversion
- Added a PowerShell race harness that runs parallel appointment creation attempts and verifies exactly one confirmed appointment plus one realtime mirror row.
- Found and fixed a real backend bug: `schedule_has_conflict` was counting the ignored appointment again through `get_occupied_ranges`, causing `update_appointment_atomic` reschedule attempts to false-conflict.

# Validation

- `pnpm backend:proof` passed.
- The command ran `supabase db reset`, applied the full migration chain, executed SQL proof assertions, and completed the parallel race check.

# Notes

- The new fix is in `supabase/migrations/20260516090000_fix_schedule_conflict_ignore.sql`, intentionally ordered after the existing scheduling/RLS/RPC hardening migrations.
- Remote smoke for the new migration remains pending.
