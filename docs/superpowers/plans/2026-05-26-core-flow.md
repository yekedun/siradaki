# Core Flow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistemi uçtan uca çalışır hale getir — Google OAuth, admin onay süreci, WhatsApp davet linki, UI iyileştirmeleri.

**Architecture:** 5 grup halinde ilerler: (A) DB migrationları → (B) Auth/session → (C) Admin panel → (D) Invite sistemi → (E) UI özellikleri. Her grup bir öncekine bağımlı, kendi içinde bağımsız.

**Tech Stack:** Expo (bare/dev build), React Native, Supabase (Edge Functions + DB), Next.js 14 (web), `@react-native-google-signin/google-signin`, `expo-secure-store`

**GH Issues:** #57 #58 #59 #60 #61 #62 #63 #64

---

## Grup A — DB Migrationları

### Task 1: shops.status + invite_tokens migrationları

**Files:**
- Create: `supabase/migrations/20260526100000_shop_status.sql`
- Create: `supabase/migrations/20260526100001_invite_tokens.sql`

- [ ] **Step 1: shops.status migration yaz**

```sql
-- supabase/migrations/20260526100000_shop_status.sql
ALTER TABLE shops
  ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'active'
  CHECK (status IN ('pending', 'active', 'rejected'));

-- Mevcut dükkanlar aktif kalır (DEFAULT 'active' zaten halleder)
-- Yeni kayıtlar register-shop edge fn üzerinden 'pending' olarak oluşturulur

COMMENT ON COLUMN shops.status IS 'pending=admin onayı bekleniyor, active=erişim açık, rejected=reddedildi';
```

- [ ] **Step 2: invite_tokens migration yaz**

```sql
-- supabase/migrations/20260526100001_invite_tokens.sql
CREATE TABLE IF NOT EXISTS invite_tokens (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token       UUID NOT NULL UNIQUE DEFAULT gen_random_uuid(),
  shop_id     UUID NOT NULL REFERENCES shops(id) ON DELETE CASCADE,
  created_by  UUID NOT NULL REFERENCES auth.users(id),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expires_at  TIMESTAMPTZ NOT NULL DEFAULT NOW() + INTERVAL '48 hours',
  used_at     TIMESTAMPTZ,
  used_by     UUID REFERENCES auth.users(id)
);

CREATE INDEX idx_invite_tokens_token ON invite_tokens(token);
CREATE INDEX idx_invite_tokens_shop  ON invite_tokens(shop_id);

-- RLS: sadece dükkan sahibi kendi tokenlarını görebilir
ALTER TABLE invite_tokens ENABLE ROW LEVEL SECURITY;

CREATE POLICY "owner_select_own_tokens" ON invite_tokens
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM shops
      WHERE shops.id = invite_tokens.shop_id
        AND (shops.owner_user_id = auth.uid() OR shops.owner_id = auth.uid())
    )
  );

CREATE POLICY "owner_insert_tokens" ON invite_tokens
  FOR INSERT WITH CHECK (
    EXISTS (
      SELECT 1 FROM shops
      WHERE shops.id = invite_tokens.shop_id
        AND (shops.owner_user_id = auth.uid() OR shops.owner_id = auth.uid())
    )
  );
```

- [ ] **Step 3: Migrationları uygula ve doğrula**

```bash
cd "C:\Users\Emre\Berber randevu"
npx supabase db reset
# Beklenen: "Finished supabase db reset." — hata yoksa devam
```

Doğrula: Supabase Studio → shops tablosunda `status` kolonu var, `invite_tokens` tablosu var.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260526100000_shop_status.sql
git add supabase/migrations/20260526100001_invite_tokens.sql
git commit -m "feat(db): shop status + invite_tokens migrations (#57 #60)"
```

---

## Grup B — Auth & Session

### Task 2: expo-secure-store ile session persistansı

> Mevcut: `AsyncStorage` (şifrelenmemiş). Hedef: `expo-secure-store` (keychain/keystore korumalı).

**Files:**
- Modify: `apps/mobile/package.json`
- Modify: `apps/mobile/lib/supabase.ts`

- [ ] **Step 1: expo-secure-store kur**

```bash
cd "C:\Users\Emre\Berber randevu\apps\mobile"
npx expo install expo-secure-store
```

- [ ] **Step 2: supabase.ts'e SecureStore adapter ekle**

`apps/mobile/lib/supabase.ts` dosyasını şu hale getir:

```typescript
import * as SecureStore from 'expo-secure-store';
import { createClient } from '@supabase/supabase-js';
import { buildOwnerRoleFilter } from './supabase-role';

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!;

// Web ortamında (admin panel test vs) SecureStore yoktur, AsyncStorage'a düş
const secureStoreAdapter = {
  getItem:    (key: string) => SecureStore.getItemAsync(key),
  setItem:    (key: string, value: string) => SecureStore.setItemAsync(key, value),
  removeItem: (key: string) => SecureStore.deleteItemAsync(key),
};

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: secureStoreAdapter,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});

export type ShopStatus = 'pending' | 'active' | 'rejected';

export async function determineUserRole(
  userId: string
): Promise<'owner' | 'pending' | 'rejected' | 'staff' | 'new_user'> {
  const { data: shop } = await supabase
    .from('shops')
    .select('id, status')
    .or(buildOwnerRoleFilter(userId))
    .maybeSingle();

  if (shop) {
    if (shop.status === 'active') return 'owner';
    if (shop.status === 'pending') return 'pending';
    if (shop.status === 'rejected') return 'rejected';
  }

  const { data: barber } = await supabase
    .from('staff')
    .select('id')
    .eq('user_id', userId)
    .eq('is_active', true)
    .maybeSingle();

  if (barber) return 'staff';
  return 'new_user';
}
```

- [ ] **Step 3: Tip testini güncelle**

`apps/mobile/__tests__/supabase-role.test.ts` dosyasını aç, mevcut test varsa geçtiğini doğrula:

```bash
cd "C:\Users\Emre\Berber randevu\apps\mobile"
npx jest __tests__/supabase-role.test.ts
```

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/supabase.ts apps/mobile/package.json
git commit -m "feat(auth): expo-secure-store session persistence, determineUserRole pending/rejected (#61)"
```

---

### Task 3: Google OAuth — native sign-in kurulumu

> `@react-native-google-signin/google-signin` — tarayıcı açmadan native Google hesap seçici.

**Files:**
- Modify: `apps/mobile/package.json`
- Modify: `apps/mobile/app.config.ts`

- [ ] **Step 1: Paketi kur**

```bash
cd "C:\Users\Emre\Berber randevu\apps\mobile"
npx expo install @react-native-google-signin/google-signin
```

- [ ] **Step 2: Supabase Dashboard — Google provider aç**

Supabase Studio → Authentication → Providers → Google → Enable.
- Client ID: Google Cloud Console'dan alınacak (Android client ID)
- Client Secret: Web client secret
- Callback URL'yi kopyala: `https://[project-ref].supabase.co/auth/v1/callback`

- [ ] **Step 3: Google Cloud Console — OAuth client oluştur**

1. console.cloud.google.com → APIs & Services → Credentials
2. "Create Credentials" → OAuth 2.0 Client ID
3. Android: Package name = `com.siradaki.app`, SHA-1 parmak izi (keystore'dan)
4. iOS: Bundle ID = `com.siradaki.app`
5. Web: Authorized redirect URI = Supabase callback URL

SHA-1 için debug keystore:
```bash
cd apps/mobile/android
./gradlew signingReport 2>/dev/null | grep -A2 "Variant: debug" | grep SHA1
```

- [ ] **Step 4: app.config.ts'e plugin ekle**

`apps/mobile/app.config.ts` içinde `plugins` dizisine ekle:

```typescript
[
  '@react-native-google-signin/google-signin',
  {
    iosUrlScheme: 'com.googleusercontent.apps.YOUR_IOS_CLIENT_ID',
  },
],
```

> `YOUR_IOS_CLIENT_ID`: Google Cloud Console'dan iOS client ID'nin "Reversed client ID" kısmı. Android için ekstra config gerekmez — SHA-1 ile Google tanır.

- [ ] **Step 5: lib/google-auth.ts helper dosyası oluştur**

```typescript
// apps/mobile/lib/google-auth.ts
import { GoogleSignin } from '@react-native-google-signin/google-signin';
import { supabase } from './supabase';

export function configureGoogleSignIn() {
  GoogleSignin.configure({
    webClientId: process.env.EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID!,
    offlineAccess: false,
  });
}

export async function signInWithGoogle(): Promise<{ error?: string }> {
  try {
    await GoogleSignin.hasPlayServices({ showPlayServicesUpdateDialog: true });
    const userInfo = await GoogleSignin.signIn();
    const idToken = userInfo.data?.idToken ?? (userInfo as any).idToken;
    if (!idToken) return { error: 'Google token alınamadı' };

    const { error } = await supabase.auth.signInWithIdToken({
      provider: 'google',
      token: idToken,
    });
    if (error) return { error: error.message };
    return {};
  } catch (e: any) {
    if (e.code === 'SIGN_IN_CANCELLED') return { error: 'İptal edildi' };
    return { error: 'Google ile giriş başarısız' };
  }
}
```

- [ ] **Step 6: .env.local'e key ekle**

`apps/mobile/.env.local` dosyasına ekle:
```
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=YOUR_WEB_CLIENT_ID.apps.googleusercontent.com
```

- [ ] **Step 7: Dev build yeniden derle (native module eklendi)**

```bash
cd "C:\Users\Emre\Berber randevu\apps\mobile"
npx expo run:android
```

Beklenen: Build başarıyla tamamlanır.

- [ ] **Step 8: Commit**

```bash
git add apps/mobile/app.config.ts apps/mobile/lib/google-auth.ts apps/mobile/package.json
git commit -m "feat(auth): @react-native-google-signin/google-signin kurulum + helper (#59)"
```

---

### Task 4: login.tsx — Google ile Giriş Yap butonu

**Files:**
- Modify: `apps/mobile/app/(auth)/login.tsx`

- [ ] **Step 1: Login ekranına Google butonu ekle**

`apps/mobile/app/(auth)/login.tsx` içinde:

1. Import ekle (dosyanın üstüne):
```typescript
import { configureGoogleSignIn, signInWithGoogle } from '../../lib/google-auth';
```

2. Component başında configure çağır:
```typescript
export default function LoginScreen() {
  configureGoogleSignIn(); // sadece bir kez çalışır, idempotent
  // ... mevcut state'ler
```

3. `handleLogin` fonksiyonunun altına ekle:
```typescript
async function handleGoogleLogin() {
  setLoading(true);
  setError(null);
  try {
    const result = await signInWithGoogle();
    if (result.error) { setError(result.error); return; }
    // _layout.tsx auth state change'i yakalar, routing oradan yapılır
  } finally {
    setLoading(false);
  }
}
```

4. CTA bölümüne, "Giriş Yap" butonunun ÜSTÜNE ekle:
```tsx
<Button
  variant="primary"
  size="lg"
  full
  disabled={loading}
  onPress={handleGoogleLogin}
>
  {loading ? 'Giriş yapılıyor…' : 'Google ile Giriş Yap'}
</Button>

<View style={styles.divider}>
  <View style={styles.dividerLine} />
  <Text style={styles.dividerText}>veya</Text>
  <View style={styles.dividerLine} />
</View>
```

5. StyleSheet'e ekle:
```typescript
divider: { flexDirection: 'row', alignItems: 'center', gap: 8, marginVertical: 4 },
dividerLine: { flex: 1, height: 1, backgroundColor: colors.slate[200] },
dividerText: { fontSize: 12, fontFamily: 'Montserrat-Regular', color: colors.slate[400] },
```

- [ ] **Step 2: Fiziksel cihazda test et**

Android'de uygulama aç → "Google ile Giriş Yap" → Google hesap seçici açılır (tarayıcı değil).

- [ ] **Step 3: Commit**

```bash
git add "apps/mobile/app/(auth)/login.tsx"
git commit -m "feat(auth): Google ile Giriş Yap butonu login ekranına eklendi (#59)"
```

---

### Task 5: Yeni kullanıcı akışı — google-onboarding.tsx + pending.tsx

> Google ile ilk kez giriş yapan kullanıcıya telefon + dükkan adı sorulur → pending ekranı.

**Files:**
- Create: `apps/mobile/app/(auth)/google-onboarding.tsx`
- Create: `apps/mobile/app/(auth)/pending.tsx`
- Create: `supabase/functions/register-shop/index.ts`
- Modify: `apps/mobile/app/_layout.tsx`

- [ ] **Step 1: register-shop edge fonksiyonu yaz**

```typescript
// supabase/functions/register-shop/index.ts
import { serve } from "https://deno.land/std@0.208.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { createAdminClient } from "../_shared/supabase-admin.ts";
import { corsOptions, error, json } from "../_shared/cors.ts";

function toSlug(name: string): string {
  return name
    .toLowerCase()
    .replace(/[çÇ]/g, "c").replace(/[ğĞ]/g, "g").replace(/[ıİ]/g, "i")
    .replace(/[öÖ]/g, "o").replace(/[şŞ]/g, "s").replace(/[üÜ]/g, "u")
    .replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 60);
}

serve(async (req) => {
  if (req.method === "OPTIONS") return corsOptions();
  if (req.method !== "POST") return error("Method not allowed", 405);

  const authHeader = req.headers.get("Authorization");
  if (!authHeader) return error("Authorization header eksik", 401);

  const anonClient = createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_ANON_KEY")!
  );
  const { data: { user }, error: authErr } = await anonClient.auth.getUser(
    authHeader.replace("Bearer ", "")
  );
  if (authErr || !user) return error("Kimlik doğrulama başarısız", 401);

  const admin = createAdminClient();

  // Zaten bir dükkanı varsa engelle
  const { data: existing } = await admin.from("shops")
    .select("id").or(`owner_user_id.eq.${user.id},owner_id.eq.${user.id}`)
    .maybeSingle();
  if (existing) return error("Bu hesaba zaten dükkan bağlı", 409);

  let body: { shop_name: string; phone: string };
  try { body = await req.json(); } catch { return error("Geçersiz JSON"); }

  const { shop_name, phone } = body;
  if (!shop_name?.trim()) return error("Dükkan adı zorunlu");
  if (!phone?.trim()) return error("Telefon zorunlu");

  // Slug çakışmasız üret
  const baseSlug = toSlug(shop_name.trim());
  let slug = baseSlug;
  let suffix = 2;
  while (true) {
    const { data: s } = await admin.from("shops").select("id").eq("slug", slug).maybeSingle();
    if (!s) break;
    slug = `${baseSlug}-${suffix++}`;
  }

  const { data: shop, error: insertErr } = await admin.from("shops").insert({
    name: shop_name.trim(),
    slug,
    owner_user_id: user.id,
    status: "pending",
  }).select("id, slug, status").single();

  if (insertErr) return error("Dükkan oluşturulamadı: " + insertErr.message, 500);

  // Sahibi staff tablosuna da ekle (kendisi de berber)
  const ownerSlug = toSlug(user.user_metadata?.full_name ?? shop_name.trim());
  await admin.from("staff").insert({
    shop_id: shop.id,
    user_id: user.id,
    name: user.user_metadata?.full_name ?? shop_name.trim(),
    phone: phone.trim(),
    role: "owner",
    is_active: true,
    slug: ownerSlug || null,
  });

  // Admin bildirim (push token Supabase secret'ta)
  const adminToken = Deno.env.get("ADMIN_EXPO_PUSH_TOKEN");
  if (adminToken) {
    await fetch("https://exp.host/--/api/v2/push/send", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        to: adminToken,
        title: "Yeni Dükkan Başvurusu",
        body: `${shop_name.trim()} — onay bekliyor`,
        data: { type: "new_shop", shopId: shop.id },
      }),
    }).catch(() => {});
  }

  // Admin email (Resend)
  const resendKey = Deno.env.get("RESEND_API_KEY");
  if (resendKey) {
    await fetch("https://api.resend.com/emails", {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${resendKey}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        from: "sistem@siradaki.app",
        to: "emreyek29@gmail.com",
        subject: `Yeni başvuru: ${shop_name.trim()}`,
        html: `<p><b>${shop_name.trim()}</b> dükkanı onay bekliyor.</p>
               <p>Telefon: ${phone}</p>
               <p>Slug: ${slug}</p>
               <p><a href="https://siradaki.app/admin">Admin panelini aç →</a></p>`,
      }),
    }).catch(() => {});
  }

  return json({ shop }, 201);
});
```

- [ ] **Step 2: google-onboarding.tsx yaz**

```typescript
// apps/mobile/app/(auth)/google-onboarding.tsx
import React, { useState } from 'react';
import {
  View, Text, StyleSheet, KeyboardAvoidingView, Platform, ScrollView,
} from 'react-native';
import { router } from 'expo-router';
import { colors } from '../../lib/theme';
import { Button } from '../../components/ds/Button';
import { TextField } from '../../components/ds/TextField';
import { supabase } from '../../lib/supabase';

const FN_BASE = process.env.EXPO_PUBLIC_SUPABASE_URL + '/functions/v1';

export default function GoogleOnboardingScreen() {
  const [shopName, setShopName] = useState('');
  const [phone,    setPhone]    = useState('');
  const [loading,  setLoading]  = useState(false);
  const [error,    setError]    = useState<string | null>(null);

  const canSubmit = shopName.trim().length >= 2 && phone.trim().length >= 10;

  async function handleSubmit() {
    if (!canSubmit || loading) return;
    setLoading(true);
    setError(null);
    try {
      const session = (await supabase.auth.getSession()).data.session;
      const res = await fetch(`${FN_BASE}/register-shop`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${session?.access_token}`,
        },
        body: JSON.stringify({ shop_name: shopName.trim(), phone: phone.trim() }),
      });
      if (!res.ok) {
        const d = await res.json().catch(() => ({}));
        setError(d.error ?? 'Kayıt oluşturulamadı');
        return;
      }
      router.replace('/(auth)/pending');
    } finally {
      setLoading(false);
    }
  }

  return (
    <KeyboardAvoidingView style={{ flex: 1, backgroundColor: colors.slate[0] }}
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>
      <ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">
        <View style={styles.topArea}>
          <View style={styles.mark}><Text style={styles.markLetter}>S</Text></View>
          <Text style={styles.overline}>Berber · Dükkan Paneli</Text>
          <Text style={styles.title}>Dükkanını Tanıt</Text>
          <Text style={styles.lead}>
            Müşteriler dükkanını bu isimle bulacak.
          </Text>
        </View>
        <View style={styles.fields}>
          <TextField label="Dükkan Adı" value={shopName} onChangeText={setShopName}
            placeholder="örn. Neco Kuaför" />
          <TextField label="Telefon Numarası" value={phone} onChangeText={setPhone}
            placeholder="05XX XXX XX XX" keyboardType="phone-pad" />
        </View>
        <View style={{ flex: 1, minHeight: 24 }} />
        <View style={styles.cta}>
          {error ? <Text style={styles.errorText}>{error}</Text> : null}
          <Button variant="primary" size="lg" full
            disabled={!canSubmit || loading} onPress={handleSubmit}>
            {loading ? 'Kaydediliyor…' : 'Devam Et'}
          </Button>
        </View>
      </ScrollView>
    </KeyboardAvoidingView>
  );
}

const styles = StyleSheet.create({
  content:  { flexGrow: 1, paddingHorizontal: 20, paddingTop: 20, paddingBottom: 28 },
  topArea:  { marginTop: 60 },
  mark:     { width: 48, height: 48, borderRadius: 999, backgroundColor: colors.ink[900],
               alignItems: 'center', justifyContent: 'center', marginBottom: 28 },
  markLetter: { fontSize: 22, fontFamily: 'Montserrat-Bold', color: '#fff' },
  overline: { fontSize: 11, fontFamily: 'Montserrat-SemiBold', letterSpacing: 1.76,
               textTransform: 'uppercase', color: colors.slate[500] },
  title:    { fontSize: 34, fontFamily: 'Montserrat-Bold', letterSpacing: -0.68,
               lineHeight: 36, color: colors.ink[900], marginTop: 14, marginBottom: 10 },
  lead:     { fontSize: 16, fontFamily: 'Montserrat-Regular', lineHeight: 25, color: colors.slate[700] },
  fields:   { marginTop: 32, gap: 14 },
  cta:      { gap: 14, alignItems: 'center' },
  errorText:{ fontSize: 13, fontFamily: 'Montserrat-Regular', color: colors.coral[600], textAlign: 'center' },
});
```

- [ ] **Step 3: pending.tsx yaz**

```typescript
// apps/mobile/app/(auth)/pending.tsx
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { colors } from '../../lib/theme';
import { Button } from '../../components/ds/Button';
import { supabase } from '../../lib/supabase';

export default function PendingScreen() {
  async function handleLogout() {
    await supabase.auth.signOut();
    // _layout.tsx session change'i yakalar → login'e yönlendirir
  }

  return (
    <View style={styles.screen}>
      <View style={styles.iconWrap}>
        <Text style={styles.icon}>⏳</Text>
      </View>
      <Text style={styles.title}>Başvurun Alındı</Text>
      <Text style={styles.body}>
        Dükkanın inceleme sürecinde. Onaylandıktan sonra bildirim alacaksın.
        Genellikle 24 saat içinde yanıt verilir.
      </Text>
      <Button variant="secondary" size="md" onPress={handleLogout}>
        Çıkış Yap
      </Button>
    </View>
  );
}

const styles = StyleSheet.create({
  screen:  { flex: 1, backgroundColor: colors.slate[0], alignItems: 'center',
              justifyContent: 'center', paddingHorizontal: 32, gap: 16 },
  iconWrap:{ width: 72, height: 72, borderRadius: 999, backgroundColor: colors.slate[100],
              alignItems: 'center', justifyContent: 'center', marginBottom: 8 },
  icon:    { fontSize: 32 },
  title:   { fontSize: 24, fontFamily: 'Montserrat-Bold', color: colors.ink[900],
              textAlign: 'center' },
  body:    { fontSize: 15, fontFamily: 'Montserrat-Regular', color: colors.slate[600],
              textAlign: 'center', lineHeight: 22 },
});
```

- [ ] **Step 4: _layout.tsx'i güncelle — pending ve new_user routing**

`apps/mobile/app/_layout.tsx` dosyasında `determineUserRole` çağrısının olduğu bloğu güncelle:

```typescript
determineUserRole(session.user.id).then(role => {
  if (role === 'owner')     router.replace('/(owner)');
  else if (role === 'staff') router.replace('/(app)');
  else if (role === 'pending')   router.replace('/(auth)/pending');
  else if (role === 'rejected')  router.replace('/(auth)/pending'); // rejected de aynı ekranı gösterir
  else router.replace('/(auth)/google-onboarding'); // new_user → dükkan kur
});
```

- [ ] **Step 5: Edge fn deploy et**

```bash
cd "C:\Users\Emre\Berber randevu"
npx supabase functions deploy register-shop
npx supabase secrets set ADMIN_EXPO_PUSH_TOKEN=ExponentPushToken[...] # kendi token'ın
```

Resend API key için (isteğe bağlı, push yeterli):
```bash
npx supabase secrets set RESEND_API_KEY=re_...
```

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/register-shop/
git add "apps/mobile/app/(auth)/google-onboarding.tsx"
git add "apps/mobile/app/(auth)/pending.tsx"
git add "apps/mobile/app/_layout.tsx"
git commit -m "feat(auth): owner registration flow — google-onboarding, pending screen, register-shop fn (#57)"
```

---

## Grup C — Admin Paneli

### Task 6: siradaki.app/admin — onay paneli

**Files:**
- Create: `apps/web/src/app/admin/page.tsx`
- Create: `apps/web/src/app/admin/layout.tsx`
- Create: `apps/web/src/app/admin/actions.ts`

- [ ] **Step 1: Admin layout (basit auth guard)**

```typescript
// apps/web/src/app/admin/layout.tsx
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ minHeight: '100vh', background: '#F7F8FA', fontFamily: 'system-ui, sans-serif' }}>
      <div style={{ background: '#1E3A8A', color: '#fff', padding: '12px 24px', fontSize: 14, fontWeight: 600 }}>
        Sıradaki Admin
      </div>
      <div style={{ maxWidth: 900, margin: '0 auto', padding: '32px 16px' }}>
        {children}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Server actions yaz**

```typescript
// apps/web/src/app/admin/actions.ts
'use server';

import { createClient } from '@supabase/supabase-js';

function getAdminClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
  );
}

function assertAdmin(adminKey: string) {
  if (adminKey !== process.env.ADMIN_SECRET_KEY) throw new Error('Yetkisiz');
}

export async function approveShop(shopId: string, adminKey: string) {
  assertAdmin(adminKey);
  const supabase = getAdminClient();
  await supabase.from('shops').update({ status: 'active' }).eq('id', shopId);
  // Sahibin push tokenını bul ve bildirim gönder
  const { data: shop } = await supabase
    .from('shops').select('owner_user_id').eq('id', shopId).single();
  if (shop?.owner_user_id) {
    const { data: token } = await supabase
      .from('push_tokens').select('token').eq('user_id', shop.owner_user_id).maybeSingle();
    if (token?.token) {
      await fetch('https://exp.host/--/api/v2/push/send', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          to: token.token,
          title: 'Başvurunuz Onaylandı! 🎉',
          body: 'Dükkanınız aktif hale getirildi. Şimdi giriş yapabilirsiniz.',
        }),
      });
    }
  }
}

export async function rejectShop(shopId: string, adminKey: string) {
  assertAdmin(adminKey);
  const supabase = getAdminClient();
  await supabase.from('shops').update({ status: 'rejected' }).eq('id', shopId);
}

export async function getPendingShops(adminKey: string) {
  assertAdmin(adminKey);
  const supabase = getAdminClient();
  const { data } = await supabase
    .from('shops')
    .select('id, name, slug, status, created_at, owner_user_id')
    .in('status', ['pending', 'active', 'rejected'])
    .order('created_at', { ascending: false })
    .limit(50);
  return data ?? [];
}
```

- [ ] **Step 3: Admin sayfasını yaz**

```typescript
// apps/web/src/app/admin/page.tsx
'use client';

import { useState, useEffect, useTransition } from 'react';
import { approveShop, rejectShop, getPendingShops } from './actions';

type Shop = { id: string; name: string; slug: string; status: string; created_at: string };

const STATUS_COLOR: Record<string, string> = {
  pending:  '#F59E0B',
  active:   '#10B981',
  rejected: '#EF4444',
};

export default function AdminPage() {
  const [key,     setKey]     = useState('');
  const [authed,  setAuthed]  = useState(false);
  const [shops,   setShops]   = useState<Shop[]>([]);
  const [isPending, startTransition] = useTransition();

  async function load(adminKey: string) {
    try {
      const data = await getPendingShops(adminKey);
      setShops(data);
      setAuthed(true);
    } catch { setAuthed(false); alert('Hatalı anahtar'); }
  }

  function handleApprove(shopId: string) {
    startTransition(async () => {
      await approveShop(shopId, key);
      await load(key);
    });
  }

  function handleReject(shopId: string) {
    startTransition(async () => {
      await rejectShop(shopId, key);
      await load(key);
    });
  }

  if (!authed) return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 12, maxWidth: 400 }}>
      <h2 style={{ margin: 0 }}>Admin Girişi</h2>
      <input
        type="password"
        placeholder="Admin anahtarı"
        value={key}
        onChange={e => setKey(e.target.value)}
        onKeyDown={e => e.key === 'Enter' && load(key)}
        style={{ padding: '10px 14px', fontSize: 15, border: '1px solid #CBD5E1', borderRadius: 8 }}
      />
      <button onClick={() => load(key)}
        style={{ padding: '10px 20px', background: '#1E3A8A', color: '#fff',
                  border: 'none', borderRadius: 8, cursor: 'pointer', fontSize: 14 }}>
        Giriş
      </button>
    </div>
  );

  const pending = shops.filter(s => s.status === 'pending');
  const rest    = shops.filter(s => s.status !== 'pending');

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 32 }}>
      <div>
        <h2 style={{ margin: '0 0 16px' }}>Bekleyen Başvurular ({pending.length})</h2>
        {pending.length === 0 && <p style={{ color: '#64748B' }}>Bekleyen başvuru yok.</p>}
        {pending.map(shop => (
          <ShopRow key={shop.id} shop={shop} onApprove={handleApprove}
            onReject={handleReject} disabled={isPending} />
        ))}
      </div>
      <div>
        <h2 style={{ margin: '0 0 16px' }}>Tüm Kayıtlar</h2>
        {rest.map(shop => (
          <ShopRow key={shop.id} shop={shop} onApprove={handleApprove}
            onReject={handleReject} disabled={isPending} />
        ))}
      </div>
    </div>
  );
}

function ShopRow({ shop, onApprove, onReject, disabled }: {
  shop: Shop; onApprove: (id: string) => void;
  onReject: (id: string) => void; disabled: boolean;
}) {
  return (
    <div style={{ background: '#fff', border: '1px solid #E2E8F0', borderRadius: 10,
                   padding: '16px 20px', marginBottom: 10, display: 'flex',
                   alignItems: 'center', gap: 16 }}>
      <div style={{ flex: 1 }}>
        <div style={{ fontWeight: 600, fontSize: 15 }}>{shop.name}</div>
        <div style={{ fontSize: 13, color: '#64748B', marginTop: 2 }}>
          siradaki.app/{shop.slug} · {new Date(shop.created_at).toLocaleString('tr-TR')}
        </div>
      </div>
      <span style={{ background: STATUS_COLOR[shop.status] + '22',
                      color: STATUS_COLOR[shop.status], padding: '3px 10px',
                      borderRadius: 20, fontSize: 12, fontWeight: 600 }}>
        {shop.status === 'pending' ? 'Bekliyor' : shop.status === 'active' ? 'Aktif' : 'Reddedildi'}
      </span>
      {shop.status === 'pending' && (
        <>
          <button disabled={disabled} onClick={() => onApprove(shop.id)}
            style={{ padding: '7px 16px', background: '#10B981', color: '#fff',
                      border: 'none', borderRadius: 7, cursor: 'pointer', fontSize: 13 }}>
            Onayla
          </button>
          <button disabled={disabled} onClick={() => onReject(shop.id)}
            style={{ padding: '7px 16px', background: '#EF4444', color: '#fff',
                      border: 'none', borderRadius: 7, cursor: 'pointer', fontSize: 13 }}>
            Reddet
          </button>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Web env vars ekle**

`apps/web/.env.local` dosyasına:
```
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
ADMIN_SECRET_KEY=gizli-bir-sifre-burayi-degistir
```

- [ ] **Step 5: Dev'de test et**

```bash
cd "C:\Users\Emre\Berber randevu\apps\web"
npm run dev
# http://localhost:3000/admin aç → admin şifresini gir → pending dükkanları gör
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/app/admin/
git commit -m "feat(admin): shop onay paneli — /admin route, onayla/reddet, push bildirim (#58)"
```

---

## Grup D — Davet Sistemi

### Task 7: invite-barber edge fn — email'den token'a geçiş

**Files:**
- Modify: `supabase/functions/invite-barber/index.ts`

- [ ] **Step 1: Edge fn'yi token moduna güncelle**

`supabase/functions/invite-barber/index.ts` içeriğini şu şekilde değiştir:

```typescript
import { serve } from "https://deno.land/std@0.208.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { createAdminClient } from "../_shared/supabase-admin.ts";
import { corsOptions, error, json } from "../_shared/cors.ts";

serve(async (req) => {
  if (req.method === "OPTIONS") return corsOptions();
  if (req.method !== "POST") return error("Method not allowed", 405);

  const authHeader = req.headers.get("Authorization");
  if (!authHeader) return error("Authorization header eksik", 401);

  const anonClient = createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_ANON_KEY")!
  );
  const { data: { user }, error: authErr } = await anonClient.auth.getUser(
    authHeader.replace("Bearer ", "")
  );
  if (authErr || !user) return error("Kimlik doğrulama başarısız", 401);

  const admin = createAdminClient();

  const { data: shop } = await admin.from("shops")
    .select("id").or(`owner_user_id.eq.${user.id},owner_id.eq.${user.id}`).single();
  if (!shop) return error("Dükkan sahibi yetkisi gerekli", 403);

  // Token oluştur (48 saat geçerli)
  const { data: tokenRow, error: tokenErr } = await admin
    .from("invite_tokens")
    .insert({ shop_id: shop.id, created_by: user.id })
    .select("token")
    .single();

  if (tokenErr) return error("Token oluşturulamadı: " + tokenErr.message, 500);

  const inviteLink = `siradaki://invite/${tokenRow.token}`;

  return json({ invite_link: inviteLink, token: tokenRow.token }, 201);
});
```

- [ ] **Step 2: Deploy et**

```bash
npx supabase functions deploy invite-barber
```

- [ ] **Step 3: Commit**

```bash
git add supabase/functions/invite-barber/index.ts
git commit -m "feat(invite): email'den WhatsApp deep link token'ına geçiş (#60)"
```

---

### Task 8: Mobil deep link handler — invite/[token].tsx

**Files:**
- Create: `apps/mobile/app/invite/[token].tsx`

- [ ] **Step 1: Deep link route yaz**

```typescript
// apps/mobile/app/invite/[token].tsx
import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, ActivityIndicator } from 'react-native';
import { useLocalSearchParams, router } from 'expo-router';
import { colors } from '../../lib/theme';
import { Button } from '../../components/ds/Button';
import { supabase } from '../../lib/supabase';
import { configureGoogleSignIn, signInWithGoogle } from '../../lib/google-auth';

const FN_BASE = process.env.EXPO_PUBLIC_SUPABASE_URL + '/functions/v1';

export default function InviteScreen() {
  const { token } = useLocalSearchParams<{ token: string }>();
  const [state,   setState]   = useState<'checking' | 'ready' | 'signing' | 'error' | 'done'>('checking');
  const [message, setMessage] = useState('');

  configureGoogleSignIn();

  useEffect(() => {
    validateToken();
  }, [token]);

  async function validateToken() {
    const { data } = await supabase
      .from('invite_tokens')
      .select('id, used_at, expires_at')
      .eq('token', token)
      .maybeSingle();

    if (!data) { setState('error'); setMessage('Davet linki geçersiz.'); return; }
    if (data.used_at) { setState('error'); setMessage('Bu davet linki daha önce kullanılmış.'); return; }
    if (new Date(data.expires_at) < new Date()) {
      setState('error'); setMessage('Davet linkinin süresi dolmuş. Sahipten yeni link isteyin.'); return;
    }
    setState('ready');
  }

  async function handleGoogleSignIn() {
    setState('signing');
    const result = await signInWithGoogle();
    if (result.error) {
      setState('error');
      setMessage(result.error);
      return;
    }

    // Token'ı kullanılmış olarak işaretle + staff kaydı için fn çağır
    const session = (await supabase.auth.getSession()).data.session;
    const res = await fetch(`${FN_BASE}/accept-invite`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${session?.access_token}`,
      },
      body: JSON.stringify({ token }),
    });

    if (!res.ok) {
      const d = await res.json().catch(() => ({}));
      setState('error');
      setMessage(d.error ?? 'Davet kabul edilemedi');
      return;
    }

    // _layout.tsx yeniden role belirler → /(app)'e yönlendirir
    router.replace('/');
  }

  if (state === 'checking') return (
    <View style={styles.center}>
      <ActivityIndicator size="large" color={colors.brand[600]} />
      <Text style={styles.sub}>Davet kontrol ediliyor…</Text>
    </View>
  );

  if (state === 'error') return (
    <View style={styles.center}>
      <Text style={styles.icon}>❌</Text>
      <Text style={styles.title}>Geçersiz Davet</Text>
      <Text style={styles.sub}>{message}</Text>
    </View>
  );

  if (state === 'done') return (
    <View style={styles.center}>
      <Text style={styles.icon}>✅</Text>
      <Text style={styles.title}>Katıldınız!</Text>
    </View>
  );

  return (
    <View style={styles.center}>
      <View style={styles.mark}><Text style={styles.markLetter}>S</Text></View>
      <Text style={styles.title}>Berber Olarak Katıl</Text>
      <Text style={styles.sub}>
        Sıradaki'ye berber olarak eklendiniz. Devam etmek için Google hesabınızla giriş yapın.
      </Text>
      <Button variant="primary" size="lg" full
        disabled={state === 'signing'}
        onPress={handleGoogleSignIn}>
        {state === 'signing' ? 'Giriş yapılıyor…' : 'Google ile Devam Et'}
      </Button>
    </View>
  );
}

const styles = StyleSheet.create({
  center:     { flex: 1, backgroundColor: colors.slate[0], alignItems: 'center',
                 justifyContent: 'center', padding: 32, gap: 16 },
  mark:       { width: 56, height: 56, borderRadius: 999, backgroundColor: colors.ink[900],
                 alignItems: 'center', justifyContent: 'center', marginBottom: 8 },
  markLetter: { fontSize: 24, fontFamily: 'Montserrat-Bold', color: '#fff' },
  icon:       { fontSize: 48 },
  title:      { fontSize: 24, fontFamily: 'Montserrat-Bold', color: colors.ink[900],
                 textAlign: 'center' },
  sub:        { fontSize: 15, fontFamily: 'Montserrat-Regular', color: colors.slate[600],
                 textAlign: 'center', lineHeight: 22 },
});
```

- [ ] **Step 2: accept-invite edge fn yaz**

```typescript
// supabase/functions/accept-invite/index.ts
import { serve } from "https://deno.land/std@0.208.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { createAdminClient } from "../_shared/supabase-admin.ts";
import { corsOptions, error, json } from "../_shared/cors.ts";

function toSlug(name: string): string {
  return name.toLowerCase()
    .replace(/[çÇ]/g, "c").replace(/[ğĞ]/g, "g").replace(/[ıİ]/g, "i")
    .replace(/[öÖ]/g, "o").replace(/[şŞ]/g, "s").replace(/[üÜ]/g, "u")
    .replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 60);
}

serve(async (req) => {
  if (req.method === "OPTIONS") return corsOptions();
  if (req.method !== "POST") return error("Method not allowed", 405);

  const authHeader = req.headers.get("Authorization");
  if (!authHeader) return error("Authorization header eksik", 401);

  const anonClient = createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_ANON_KEY")!
  );
  const { data: { user }, error: authErr } = await anonClient.auth.getUser(
    authHeader.replace("Bearer ", "")
  );
  if (authErr || !user) return error("Kimlik doğrulama başarısız", 401);

  const admin = createAdminClient();

  let body: { token: string };
  try { body = await req.json(); } catch { return error("Geçersiz JSON"); }

  const { token } = body;
  if (!token) return error("Token zorunlu");

  // Token doğrula
  const { data: inviteRow } = await admin
    .from("invite_tokens")
    .select("id, shop_id, used_at, expires_at")
    .eq("token", token)
    .maybeSingle();

  if (!inviteRow) return error("Geçersiz token", 404);
  if (inviteRow.used_at) return error("Token zaten kullanılmış", 409);
  if (new Date(inviteRow.expires_at) < new Date()) return error("Token süresi dolmuş", 410);

  // Token'ı kullanıldı olarak işaretle
  await admin.from("invite_tokens")
    .update({ used_at: new Date().toISOString(), used_by: user.id })
    .eq("id", inviteRow.id);

  // Zaten staff varsa sadece token'ı işaretle
  const { data: existing } = await admin.from("staff")
    .select("id").eq("user_id", user.id).eq("shop_id", inviteRow.shop_id).maybeSingle();
  if (existing) return json({ staff: existing }, 200);

  // Slug üret
  const name = user.user_metadata?.full_name ?? user.email?.split("@")[0] ?? "berber";
  const baseSlug = toSlug(name);
  let slug = baseSlug; let suffix = 2;
  while (true) {
    const { data: s } = await admin.from("staff").select("id")
      .eq("shop_id", inviteRow.shop_id).eq("slug", slug).maybeSingle();
    if (!s) break;
    slug = `${baseSlug}-${suffix++}`;
  }

  const { data: staffMember, error: insertErr } = await admin.from("staff").insert({
    shop_id: inviteRow.shop_id,
    user_id: user.id,
    name,
    role: "staff",
    is_active: true,
    slug: slug || null,
  }).select("id, name").single();

  if (insertErr) return error("Staff kaydı oluşturulamadı: " + insertErr.message, 500);

  return json({ staff: staffMember }, 201);
});
```

- [ ] **Step 3: Deploy et**

```bash
npx supabase functions deploy accept-invite
```

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/app/invite/
git add supabase/functions/accept-invite/
git commit -m "feat(invite): deep link handler + accept-invite edge fn (#60)"
```

---

### Task 9: Team ekranı — davet linki WhatsApp ile paylaş

> Mevcut "Berber Davet Et" butonu email gönderiyor. Şimdi link üretip paylaşacak.

**Files:**
- Modify: `apps/mobile/app/(owner)/team.tsx`

- [ ] **Step 1: Davet et fonksiyonunu güncelle**

`apps/mobile/app/(owner)/team.tsx` içindeki "Personel ekle" / davet akışını bul. Şu şekilde güncelle:

Mevcut `invite-barber` çağrısı `email + display_name` gönderiyordu. Yeni versiyon sadece istek gönderir, link döner.

Dosyada davet mantığını şu şekle getir:

```typescript
// team.tsx içinde — davet et handler
async function handleInvite() {
  setInviteLoading(true);
  try {
    const session = (await supabase.auth.getSession()).data.session;
    const res = await fetch(`${FN_BASE}/invite-barber`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${session?.access_token}`,
      },
      body: JSON.stringify({}), // artık email gerekmez
    });
    if (!res.ok) { Alert.alert('Hata', 'Davet linki oluşturulamadı'); return; }
    const { invite_link } = await res.json();

    // WhatsApp'ta aç
    const msg = encodeURIComponent(
      `Sıradaki randevu uygulamasına berber olarak katılman için davet linki:\n${invite_link}`
    );
    const waUrl = `whatsapp://send?text=${msg}`;
    const canOpen = await Linking.canOpenURL(waUrl);
    if (canOpen) {
      await Linking.openURL(waUrl);
    } else {
      // WhatsApp yoksa linki panoya kopyala
      await Clipboard.setStringAsync(invite_link);
      Alert.alert('Link Kopyalandı', 'Davet linki panoya kopyalandı.');
    }
  } finally {
    setInviteLoading(false);
  }
}
```

Import ekle (dosyanın üstüne):
```typescript
import { Linking } from 'react-native';
import * as Clipboard from 'expo-clipboard';
```

Const ekle:
```typescript
const FN_BASE = process.env.EXPO_PUBLIC_SUPABASE_URL + '/functions/v1';
const [inviteLoading, setInviteLoading] = useState(false);
```

- [ ] **Step 2: "Personel Ekle" butonunu güncelle**

Mevcut modal/sheet'i (email input olan kısmı) kaldır, yerinde doğrudan `handleInvite` çağıran buton bırak:

```tsx
<TouchableOpacity
  style={[styles.addButton, inviteLoading && { opacity: 0.6 }]}
  onPress={handleInvite}
  disabled={inviteLoading}
>
  <Text style={styles.addButtonText}>
    {inviteLoading ? 'Link oluşturuluyor…' : '+ Berber Davet Et'}
  </Text>
</TouchableOpacity>
```

- [ ] **Step 3: Commit**

```bash
git add "apps/mobile/app/(owner)/team.tsx"
git commit -m "feat(invite): team ekranı WhatsApp link paylaşımı (#60)"
```

---

## Grup E — UI Özellikleri

### Task 10: Block ekranı — tüm gün kapat + hazır süreler

**Files:**
- Modify: `apps/mobile/app/(app)/block.tsx`

- [ ] **Step 1: Mevcut durations dizisini güncelle + tüm gün state ekle**

`apps/mobile/app/(app)/block.tsx` dosyasında:

1. Sabitleri güncelle:
```typescript
const PRESET_DURATIONS = [30, 60, 120]; // 30dk, 1sa, 2sa
// Özel seçeneği ayrı tut
```

2. State ekle:
```typescript
const [allDay,      setAllDay]      = useState(false);
const [customInput, setCustomInput] = useState('');
const [showCustom,  setShowCustom]  = useState(false);
```

3. Mevcut `dur` state'i "özel" durumunu destekleyecek şekilde güncelle:
```typescript
// dur: number | 'custom'
const [dur, setDur] = useState<number | 'custom'>(30);
```

Fiili süreyi hesapla:
```typescript
const effectiveDur = allDay
  ? 24 * 60  // tüm gün = 1440 dakika
  : dur === 'custom'
    ? (parseInt(customInput) || 0)
    : dur;
```

- [ ] **Step 2: Tüm gün toggle ve özel süre input'u UI'a ekle**

Mevcut "Süre" bölümünün üstüne tüm gün toggle:

```tsx
{/* Tüm gün toggle */}
<View style={styles.allDayRow}>
  <View style={{ flex: 1 }}>
    <Text style={styles.allDayTitle}>Bugünü Tamamen Kapat</Text>
    <Text style={styles.allDaySub}>Tüm gün için blok oluşturur</Text>
  </View>
  <Toggle on={allDay} onChange={setAllDay} />
</View>
```

Süre bölümünü wrap et: `{!allDay && (...)}`

Mevcut duration chip'lerini değiştir:
```tsx
{/* Preset chips: 30, 60, 120 */}
{PRESET_DURATIONS.map(d => (
  <TouchableOpacity key={d}
    style={[styles.chip, dur === d && styles.chipActive]}
    onPress={() => { setDur(d); setShowCustom(false); }}>
    <Text style={[styles.chipNum, dur === d && styles.chipNumActive]}>
      {d < 60 ? d : d / 60}
    </Text>
    <Text style={[styles.chipUnit, dur === d && styles.chipUnitActive]}>
      {d < 60 ? 'dakika' : 'saat'}
    </Text>
  </TouchableOpacity>
))}

{/* Özel chip */}
<TouchableOpacity
  style={[styles.chip, dur === 'custom' && styles.chipActive]}
  onPress={() => { setDur('custom'); setShowCustom(true); }}>
  <Text style={[styles.chipNum, dur === 'custom' && styles.chipNumActive]}>Özel</Text>
</TouchableOpacity>

{/* Özel input */}
{showCustom && (
  <View style={styles.customRow}>
    <TextField
      label="Dakika"
      value={customInput}
      onChangeText={setCustomInput}
      keyboardType="number-pad"
      placeholder="örn. 45"
    />
  </View>
)}
```

Style ekle:
```typescript
allDayRow:  { flexDirection: 'row', alignItems: 'center', paddingHorizontal: 20,
               marginBottom: 16, gap: 12 },
allDayTitle:{ fontSize: 15, fontFamily: 'Montserrat-SemiBold', color: colors.ink[900] },
allDaySub:  { fontSize: 12, fontFamily: 'Montserrat-Regular', color: colors.slate[500], marginTop: 2 },
customRow:  { paddingHorizontal: 20, marginTop: 8, width: '100%' },
```

- [ ] **Step 3: Blok gönderme mantığını güncelle**

`effectiveDur` kullanarak block çağrısı yap. `allDay` durumunda `start` = günün başı (00:00), `end` = günün sonu (23:59):

```typescript
async function handleBlock() {
  if (effectiveDur <= 0) return;
  // ...mevcut logic, sadece dur yerine effectiveDur kullan
}
```

- [ ] **Step 4: Commit**

```bash
git add "apps/mobile/app/(app)/block.tsx"
git commit -m "feat(block): tüm gün kapat + hazır süreler (30dk/1sa/2sa/Özel) (#62)"
```

---

### Task 11: Ekip düzenleme bottom sheet (StaffEditSheet)

**Files:**
- Create: `apps/mobile/components/StaffEditSheet.tsx`
- Modify: `apps/mobile/app/(owner)/team.tsx`

- [ ] **Step 1: StaffEditSheet bileşenini yaz**

```typescript
// apps/mobile/components/StaffEditSheet.tsx
import React, { useState, useEffect } from 'react';
import {
  Modal, View, Text, TextInput, StyleSheet,
  TouchableOpacity, ScrollView, Switch,
} from 'react-native';
import { colors } from '../lib/theme';
import { Button } from './ds/Button';
import { supabase } from '../lib/supabase';

interface StaffMember {
  id: string;
  name: string;
  phone?: string;
  is_active: boolean;
}

interface Props {
  staff: StaffMember | null;
  visible: boolean;
  onClose: () => void;
  onSaved: () => void;
}

export function StaffEditSheet({ staff, visible, onClose, onSaved }: Props) {
  const [name,     setName]     = useState('');
  const [phone,    setPhone]    = useState('');
  const [isActive, setIsActive] = useState(true);
  const [loading,  setLoading]  = useState(false);
  const [error,    setError]    = useState<string | null>(null);

  useEffect(() => {
    if (staff) {
      setName(staff.name);
      setPhone(staff.phone ?? '');
      setIsActive(staff.is_active);
      setError(null);
    }
  }, [staff]);

  async function handleSave() {
    if (!staff || !name.trim()) return;
    setLoading(true);
    setError(null);
    try {
      const { error: err } = await supabase
        .from('staff')
        .update({
          name: name.trim(),
          phone: phone.trim() || null,
          is_active: isActive,
        })
        .eq('id', staff.id);
      if (err) { setError(err.message); return; }
      onSaved();
      onClose();
    } finally {
      setLoading(false);
    }
  }

  if (!staff) return null;

  return (
    <Modal visible={visible} animationType="slide" transparent presentationStyle="overFullScreen">
      <TouchableOpacity style={styles.backdrop} activeOpacity={1} onPress={onClose} />
      <View style={styles.sheet}>
        {/* Handle */}
        <View style={styles.handle} />

        <Text style={styles.title}>Berber Düzenle</Text>

        <ScrollView style={{ flex: 1 }} keyboardShouldPersistTaps="handled">
          {/* Ad */}
          <View style={styles.field}>
            <Text style={styles.label}>Ad Soyad</Text>
            <TextInput
              style={styles.input}
              value={name}
              onChangeText={setName}
              placeholder="Berber adı"
              placeholderTextColor={colors.slate[400]}
            />
          </View>

          {/* Telefon */}
          <View style={styles.field}>
            <Text style={styles.label}>Telefon</Text>
            <TextInput
              style={styles.input}
              value={phone}
              onChangeText={setPhone}
              placeholder="05XX XXX XX XX"
              keyboardType="phone-pad"
              placeholderTextColor={colors.slate[400]}
            />
          </View>

          {/* Aktif/Pasif toggle — en altta */}
          <View style={styles.toggleRow}>
            <View>
              <Text style={styles.toggleLabel}>
                {isActive ? 'Aktif' : 'Pasif'}
              </Text>
              <Text style={styles.toggleSub}>
                {isActive
                  ? 'Müşteriler randevu alabilir'
                  : 'Randevu alınamaz, takvimde görünmez'}
              </Text>
            </View>
            <Switch
              value={isActive}
              onValueChange={setIsActive}
              trackColor={{ false: colors.slate[300], true: colors.brand[500] }}
              thumbColor="#fff"
            />
          </View>
        </ScrollView>

        {error && <Text style={styles.error}>{error}</Text>}

        <View style={styles.actions}>
          <Button variant="secondary" size="md" onPress={onClose}>İptal</Button>
          <Button variant="primary" size="md" disabled={loading || !name.trim()} onPress={handleSave}>
            {loading ? 'Kaydediliyor…' : 'Kaydet'}
          </Button>
        </View>
      </View>
    </Modal>
  );
}

const styles = StyleSheet.create({
  backdrop: { position: 'absolute', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.4)' },
  sheet:    { position: 'absolute', bottom: 0, left: 0, right: 0, backgroundColor: '#fff',
               borderTopLeftRadius: 20, borderTopRightRadius: 20, paddingBottom: 32,
               maxHeight: '80%', display: 'flex', flexDirection: 'column' },
  handle:   { width: 36, height: 4, borderRadius: 2, backgroundColor: colors.slate[300],
               alignSelf: 'center', marginTop: 12, marginBottom: 20 },
  title:    { fontSize: 18, fontFamily: 'Montserrat-Bold', color: colors.ink[900],
               paddingHorizontal: 20, marginBottom: 20 },
  field:    { paddingHorizontal: 20, marginBottom: 16 },
  label:    { fontSize: 12, fontFamily: 'Montserrat-SemiBold', color: colors.slate[500],
               textTransform: 'uppercase', letterSpacing: 1.2, marginBottom: 6 },
  input:    { borderWidth: 1, borderColor: colors.slate[200], borderRadius: 10, padding: 12,
               fontSize: 15, fontFamily: 'Montserrat-Regular', color: colors.ink[900] },
  toggleRow:{ flexDirection: 'row', alignItems: 'center', paddingHorizontal: 20,
               paddingVertical: 16, borderTopWidth: 1, borderTopColor: colors.slate[100],
               marginTop: 8, gap: 12 },
  toggleLabel:{ fontSize: 15, fontFamily: 'Montserrat-SemiBold', color: colors.ink[900] },
  toggleSub:  { fontSize: 12, fontFamily: 'Montserrat-Regular', color: colors.slate[500], marginTop: 2 },
  actions:  { flexDirection: 'row', gap: 12, paddingHorizontal: 20, paddingTop: 16,
               borderTopWidth: 1, borderTopColor: colors.slate[100] },
  error:    { fontSize: 13, color: colors.coral[600], paddingHorizontal: 20, marginBottom: 8 },
});
```

- [ ] **Step 2: team.tsx'e sheet'i bağla**

`apps/mobile/app/(owner)/team.tsx` içinde:

1. Import ekle:
```typescript
import { StaffEditSheet } from '../../components/StaffEditSheet';
```

2. State ekle:
```typescript
const [editStaff,   setEditStaff]   = useState<StaffMember | null>(null);
const [editVisible, setEditVisible] = useState(false);
```

3. Kart `onPress`'ini güncelle — mevcut `Alert.alert(...)` çağrısını kaldır:
```typescript
onPress={() => {
  setEditStaff(member);
  setEditVisible(true);
}}
```

4. Return'ün sonuna ekle:
```tsx
<StaffEditSheet
  staff={editStaff}
  visible={editVisible}
  onClose={() => setEditVisible(false)}
  onSaved={loadStaff} // mevcut yükleme fonksiyonu
/>
```

- [ ] **Step 3: Sahip listenin başında, etiketsiz**

`team.tsx` içinde staff listesini yükleyen sorguyu güncelle — owner her zaman ilk sırada:

```typescript
// loadStaff fonksiyonu içinde
const { data: staffData } = await supabase
  .from('staff')
  .select('id, name, phone, is_active, role')
  .eq('shop_id', shopId)
  .order('role', { ascending: false }) // 'owner' alfabetik olarak 'staff'tan önce gelir
  .order('name');
```

Eğer bu yetmezse (role sıralaması garanti vermezse), JS'te sırala:
```typescript
const sorted = (staffData ?? []).sort((a, b) => {
  if (a.role === 'owner') return -1;
  if (b.role === 'owner') return 1;
  return a.name.localeCompare(b.name, 'tr');
});
setStaffList(sorted);
```

Kart render'ında `role` etiketini **gösterme** — müşteriye "sahip" görünmez.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/components/StaffEditSheet.tsx
git add "apps/mobile/app/(owner)/team.tsx"
git commit -m "feat(team): StaffEditSheet — düzenle bottom sheet, sahip ilk sırada etiket yok (#64)"
```

---

### Task 12: Web — WhatsApp bildirim butonu + sahip ilk sırada

**Files:**
- Modify: `apps/web/src/components/BookingModal.tsx`
- Modify: `apps/web/src/app/[slug]/page.tsx`

- [ ] **Step 1: BookingModal — success state'e WhatsApp butonu ekle**

`apps/web/src/components/BookingModal.tsx` içinde success state'i bul. `summary`, `staffId` prop'larına ek olarak `staffPhone` prop'u ekle:

Interface güncelle:
```typescript
interface BookingModalProps {
  // ... mevcut props
  staffPhone?: string | null;  // yeni
}
```

Success state içine, "Tamam" butonunun altına ekle:

```tsx
{staffPhone && (
  <button
    onClick={() => {
      const msg = encodeURIComponent(
        `Merhaba, ${summary} için randevu aldım. Bilginize 🙏`
      );
      window.open(`whatsapp://send?phone=90${staffPhone.replace(/\D/g,'')}&text=${msg}`, '_self');
    }}
    style={{
      width: '100%', padding: '12px 20px', marginTop: 8,
      background: '#25D366', color: '#fff', border: 'none',
      borderRadius: 10, fontSize: 14, fontWeight: 600, cursor: 'pointer',
      display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 8,
    }}
  >
    💬 Berberi WhatsApp ile Bilgilendir
  </button>
)}
```

- [ ] **Step 2: page.tsx — staffPhone'u BookingClient'a geçir**

`apps/web/src/app/[slug]/page.tsx` içinde staff sorgusuna `phone` ekle:

```typescript
const { data: staff } = await supabase
  .from('staff')
  .select('id, name, phone, role')  // phone + role eklendi
  .eq('shop_id', shop.id)
  .eq('is_active', true);
```

Sırala — owner her zaman başta, etiketsiz:
```typescript
const sortedStaff = (staff ?? []).sort((a, b) => {
  if (a.role === 'owner') return -1;
  if (b.role === 'owner') return 1;
  return (a.name ?? '').localeCompare(b.name ?? '', 'tr');
});
```

`BookingClient`'a `sortedStaff` geçir (mevcut `staff` yerine).

- [ ] **Step 3: BookingClient — seçilen berberin phone'unu BookingModal'a geçir**

`apps/web/src/app/[slug]/BookingClient.tsx` içinde `BookingModal` çağrısına:
```tsx
staffPhone={selectedStaff?.phone ?? null}
```

`selectedStaff`'ı `staffId`'den bul:
```typescript
const selectedStaff = staff.find(s => s.id === bookingState.staffId) ?? null;
```

- [ ] **Step 4: Build test**

```bash
cd "C:\Users\Emre\Berber randevu\apps\web"
npm run build
# Hata yoksa devam
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/components/BookingModal.tsx
git add "apps/web/src/app/[slug]/page.tsx"
git add "apps/web/src/app/[slug]/BookingClient.tsx"
git commit -m "feat(web): WhatsApp bildirim butonu + sahip listede ilk sırada (#63 #64)"
```

---

## Son — GH Issues Kapat

- [ ] **Step 1: Tüm core-flow issue'larını kapat**

```bash
gh issue close 57 --comment "Admin onay süreci tamamlandı — register-shop fn, pending ekranı"
gh issue close 58 --comment "/admin paneli canlıda"
gh issue close 59 --comment "Google OAuth kurulumu tamamlandı"
gh issue close 60 --comment "WhatsApp deep link davet sistemi tamamlandı"
gh issue close 61 --comment "expo-secure-store session persist"
gh issue close 62 --comment "Tüm gün kapat + hazır süreler"
gh issue close 63 --comment "WhatsApp bildirim butonu web'e eklendi"
gh issue close 64 --comment "StaffEditSheet — düzenleme ekranı + sahip etiket yok"
```

---

## Self-Review

**Spec coverage:**
- ✅ Akış 1: shops.status migration (Task 1) + google-onboarding (Task 5) + pending ekranı (Task 5) + admin panel (Task 6)
- ✅ Akış 2: invite-barber token modu (Task 7) + deep link (Task 8) + team WhatsApp share (Task 9)
- ✅ Akış 3: expo-secure-store (Task 2) + blok özelleştirme (Task 10)
- ✅ Akış 4: WhatsApp bildirim butonu (Task 12)
- ✅ Akış 5: StaffEditSheet (Task 11) + sahip ilk sırada etiket yok (Task 11, 12)
- ✅ Google OAuth: Task 3, 4
- ✅ Admin bildirim (push + email): Task 5 içinde register-shop fn
- ✅ Slug otomatik: Task 5 içinde register-shop fn

**Gözden geçirme notları:**
- `push_tokens` tablosu `accept-invite` içinde kullanılmıyor (sadece register-shop'ta var). Berber onay push'u ileride eklenebilir.
- `StaffEditSheet` hizmet seçimi (spec'te bahsedildi) bu planda yok — çünkü servislerin staff'a ataması için ayrı bir tablo mekanizması gerekiyor. Bu Task 11'e eklenebilir ya da ayrı bir issue açılabilir.
- Admin paneli `SUPABASE_SERVICE_ROLE_KEY` gerektiriyor — `.env.local`'e eklenmeli, production'da Vercel env var olarak ayarlanmalı.
